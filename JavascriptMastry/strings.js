// :- Strings -:

const single = "1";
const double = "2";
const backtick = `${1 + 2}`;

// single & doouble are same but backtick provide extented functionaality

console.log(single, double, backtick);

const sum = (a, b) => a + b;

console.log(`sum of two number ${sum(5, 4)}`);
